apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-staging
  namespace: doe25-group-15
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: backend-staging
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
